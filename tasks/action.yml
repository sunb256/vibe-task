impl_rule: |
  # 実装ルール（可読性基準）

  1. 関数は原則 35 行以内、超える場合は貴務で分割。
  2. if はカード節優先で、ネストは最大2段まで。
  3. 変数名、関数名は長すぎる場合は、基本はMax3ワードまで、シンプルな表現を用いる。
  4. 連切な改行を行う。
  5. KISS/DRY/リーダブルコードの原則を守る

task:
  - id: '74'
    url: '-'
    title: '-'
    action: |
      CustomPrompt編集画面を作りたい
      
      本システムはcodexのCustomPrompt機能と密接な関係がある
      
      ~/.codex/prompts/*.md
      
      ファイルを一覧表示できる画面をCustom Prompt画面に作る
      
       - Project一覧と同様にリスト表示とする
       - 編集、削除がある
       - 開くとモーダルでMarkdown編集ができる
