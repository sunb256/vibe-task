services:

  backend:
    build:
      context: .
      dockerfile: src/backend/Dockerfile
    user: "${UID}:${GID}"
    ports:
      - ${BACKEND_PORT}:5000
    environment:
      - HOME=${HOME}
      # Proxy
      - HTTP_PROXY=${http_proxy}
      - HTTPS_PROXY=${http_proxy}
      - http_proxy=${http_proxy}
      - https_proxy=${http_proxy}
      - NO_PROXY=${no_proxy}${NO_PROXY_APPEND}
      - no_proxy=${no_proxy}${NO_PROXY_APPEND}
    volumes:
      - ./tasks:/app/tasks
      - ${HOME}/ghq:${HOME}/ghq
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: src/frontend/Dockerfile
    user: "${UID}:${GID}"
    environment:
      - VITE_API_PROXY_TARGET=http://backend:5000
      # Proxy
      - HTTP_PROXY=${http_proxy}
      - HTTPS_PROXY=${http_proxy}
      - http_proxy=${http_proxy}
      - https_proxy=${http_proxy}
      - NO_PROXY=${no_proxy}${NO_PROXY_APPEND}
      - no_proxy=${no.proxy}${NO_PROXY_APPEND}
    ports:
      - "${FRONTEND_PORT}:5555"
    restart: unless-stopped
    depends_on:
      - backend
